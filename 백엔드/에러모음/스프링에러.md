1. 프로젝트 import 했는데 오류 엄청많고 src폴더만 있다?
=> build path 맞춰주고 project facet 에서 자바버전 맞춰준다.


1. ooo cannot be resolved to a type
=>
1. jre라이브러리 깨짐
2. 클래스변수에 CLASSPATH가 안잡혔거나 틀렸음 : 프로젝트 우클릭 - 설정 - java build path - classpath클릭후 add external jars - 톰캣내의 lib내 servlet-api.jar로 열기.

(모든소스에는 에러없고 프로젝트에만 에러있는 상태)
라이브러리에 에러난 클래스패스를 edit해보면
org.eclipse.buildship.core.gradleclasspathcontainer값이 있음
=>결론은 .proejct파일을 열어서 facet 부분을 제거하고 새로고침하면  Project and External Dependencies 항목이 정상적으로 나온다

1. 롬복 sts에도 설치되었는데 에러 뜨면
=> gradle이나 메이븐 다시 refresh하면 없어진다.

1. ☆☆☆☆☆ cannot be resolved to a Type ☆☆☆☆☆
=> 프로젝트의 jdk버전과 import 한 프로젝트의 jdk버전이 달라서 발생할 경우가 대부분
인스턴스를 전달해줘야되는데 클래스 이름으로 전달할경우

1. ☆☆☆☆☆ ( 마이바티스 쿼리.xml에서) class/typealias not found gradle mybatis parametertype
=> https://4urdev.tistory.com/46
 xml파일을 mybatis xml로 만들고, 쿼리의 오타 발견

1. ☆☆☆☆☆beancreationexception sqlsessionfactory
=> 해결.
컨트롤러에서 Mapping받는게 중복돼어있었음

1. ☆☆☆☆☆class not found exception,  nullpointer Exception
=> 확인해야될것 크게 3개.
    1.  인터페이스 메소드와  xml의 id이름 같은지 체크
    1.  xml mapper namespace에 기재된 인터페이스의 위치. demo가 끼여있거나 진짜 패키지 경로문제다.
    1.  xml에서 사용하는 DTO클래스의 위치
    이게 다맞는데 안된다? DatabaseConfiguration확인
    SqlSessionFactory에서 Mapper위치 및 DTO설정 하게되어있음
    1. 이거까지 하고도 모르겠다? 그럼 mybatis-config 만들어주자.


=> 클래스 파일명과 소스에서 클래스명이 달랐다.

1. ☆☆☆junit 테스트할때 ☆☆the input type of the launch configuration does not exist
하면서 경고창 뜨고 설정파일창 뜸
the input type of the launch configuration does not exist
can not find test class 'com.health.MapperTests' in project 'HealthPlanner'
=>
클래스패스대로 파일있는지 확인, DBconfig의 *Mapper.xml도 확인
테스트폴더 우클릭 - build path - use as source folder 체크돼있음
롬복 getter / setter 있는지 확인
=>


1. ☆☆java.lang.IllegalStateException: Failed to load ApplicationContext
   at org.springframework.test.context.cache.DefaultCacheAwareContextLoader
  Caused by: org.springframework.beans.factory.UnsatisfiedDependencyException: Error creating bean with name 'commentServiceImpl': Unsatisfied dependency expressed through field 'commentMapper'; nested exception is org.springframework.beans.factory.NoSuchBeanDefinitionException: No qualifying bean of type 'com.coffee.mapper.CommentMapper' available: expected at least 1 bean which qualifies as autowire candidate. Dependency annotations: {@org.springframework.beans.factory.annotation.Autowired(required=true)}
=> https://nackwon.tistory.com/m/109
=> (해결) mybatis썼으면 인터페이스에 @Mapper
jpa썼으면 @Repository 빼먹었음.


1. Unable to find a @SpringBootConfiguration, you need to use @ContextConfiguration
=> @SpringBootConfiguration 클래스가존재하는 패키지의
하위 패키지에 테스트를 둬야한다는 원칙을 어긴것이다.
---
1. log4j cannot be resolved to a type
=> pom.xml열어서 log4j부분의 <scope>runtime</scope> 부분 주석처리

2. Multiple annotations found at this line
=> 프로젝트의 jre경로 재설정

3. java.lang.UnsupportedClassVersionError: com/example/thymeleaf/BoardApplicationTests has been compiled by a more recent version of the Java Runtime (class file version 55.0), this version of the Java Runtime only recognizes class file versions up to 52.0
=> Project menu > Properties > Java Compiler > Compiler compliance level > 1.8

4. java.sql.SQLNonTransientConnectionException: Public Key Retrieval is not allowed
=> mysql 8.x 버전 이후로 발생
jdbc url에 allowPublicKeyRetrieval=true&useSSL=false 필요

5. MyBatis 오류: Invalid bound statement (not found)
=> 정확한 로그는 다음과 같다.
org.apache.ibatis.binding.BindingException: Invalid bound statement (not found): com.example.thymeleaf.mapper.BoardMapper.insertBoard

보통 2가지 원인
1) Mapper 인터페이스와 Mapper xml에 오타가 있는 경우
2) Mapper xml 경로가 잘못된 경우
나는 2번의 문제였다. 실제 물리경로와 다르게 참조하는 경로에서 패키지 하나가 빠졌었다.


6. 스프링부트에서 com.fasterxml.jackson.databind.exc.InvalidDefinitionException: Java 8 date/time type `java.time.LocalDateTime` not supported by default: add Module "com.fasterxml.jackson.datatype:jackson-datatype-jsr310
=>객체의 항목 중 LocalDateTime 이 있는 항목의 값을 ObejctMapper 함수를 써서 가져올 경우 위와 같은 에러가 발생하는 경우는 Java 8에서 추가된 LocalDateTime 항목을 제대로 직렬화 또는 역직렬화를 못하는 현상.
(해결)이런 경우 메이븐리포지토리(https://mvnrepository.com/) 사이트에서 jackson-datatype-jsr310를 종속성 추가하고 new ObjectMapper().registerModule(new JavaTimeModule()).writeValueAsString("DTO객체"); 이런식으로 코드를 바꿔줘야한다.

7. java.lang.UnsupportedClassVersionError
=> 빌드한 자바버전보다 낮은 버전에서 배포할려고 할때 발생
49 = Java 5
50 = Java 6
51 = Java 7
52 = Java 8
53 = Java 9
54 = Java 10
55 = Java 11
56 = Java 12
57 = Java 13
58 = Java 14
자바 버전 올리고 프로젝트에서 높은 자바버전 참조하게 하니까 해결됨.

8. 스프링부트에 Please sign in 페이지 뜬다면?
=> 기본사용자 이름은 user고
비밀번호는 console에 뜬다.


9. the method is undefined for the type follow
=> lombock이 안깔려서 setter함수가 없다고 인식하는 문제.

10. Failed to execute goal [32morg.apache.maven.plugins:maven-war-plugin:2.2:war[m
=> 해당버전으로 maven 컴파일에 실패했다는 말
pom.xml에 명시된 maven compiler버전과 서버에 설치된 컴파일러 버전을 일치시키면 된다.

1. hikari연결.  public key retrieval is not allowed
=> application.properties
의 jdbc url 부분에 jdbc:mysql://localhost:3306/db?allowPublicKeyRetrieval=true&useSSL=false

1. org.thymeleaf.exceptions.TemplateProcessingException
=>   타임리프 사용 html소스에 신택스 에러
또는 컨트롤러의 mapping 경로 이상
=> 근데 못고침

1. [MyBatis] parametertype 에서 Invalid bound statement (not found)
=> (원인)
    1. Mapper Interface와 mapping되는 xml파일에 오타가 있는 경우 : id와 메소드 공백, 대소문자 확인
    2. classpath에 경로가 잘못된 경우 (동일한 이름의 다른 패키지경로)
    3. xml이 저장될 경로는 잘못 생성한 경우(application.properties에 mybatis.mapper-locations:classpath:mapper/*.xml 설정)
    (해결)
    resources밑에는 package를 생성못하고 directory만 생성가능하다.
    @MapperScan 은 패키지를 원한다.
---
사소한 에러

1. FAILURE: Build failed with an exception.
* What went wrong:
Could not open cp_init remapped class cache for 6a5krabdzij62jglcll542e66 (C:\Users\ggoom\.gradle\caches\6.0.1\scripts-remapped\eclipsePlugin_eu89pja1z0urssza4vskwy6b1\6a5krabdzij62jglcll542e66\cp_init27b7120d8bed45e60dd0b95b8e5ecea0).
> Could not open cp_init generic class cache for initialization script 'F:\Programming\workspace_sts4\.metadata\.plugins\org.eclipse.buildship.core\init.d\eclipsePlugin.gradle' (C:\Users\ggoom\.gradle\caches\6.0.1\scripts\6a5krabdzij62jglcll542e66\cp_init\cp_init27b7120d8bed45e60dd0b95b8e5ecea0).
   > Could not initialize class org.codehaus.groovy.classgen.Verifier
=>  원인 : GRADLE_USER_HOME설정안되어있음 또는 경로에 한글 경로가 있음
해결 : https://itbellstone.tistory.com/101
//이클립스에서 gradle 플러그인설치 후 gradle 빌드를 할경우 수동으로 설정해주지않으면
디폴트로 $USER_HOME/.gradle   를 사용한다.

1. org.thymeleaf.exceptions.TemplateInputException: Error resolving template [board/list], template might not exist or might not be accessible by any of the configured Template Resolvers
=> html파일 경로 문제였음. 경로고치면 해결됨.




1. ★ 콘솔에러 POST http://localhost:8080/board/delete.do 403
=> 해결한 결과는 아래의 것 지지고볶고 다했는데 다 문제아니였고 스프링설정파일 gradle중복때문에 아예 설정파일이 먹히지 않아서였다. meta정보나 input hidden이나 아무것도 필요없음.
설정파일에 println찍어봤는데 안찍혀서 알아냄.
자바파일인데 src/main/java에 안넣어뒀어서 그랬음
추상클래스 WebSecurityConfigurerAdapter 상속받아서 	@Override
	protected void configure(HttpSecurity httpSecurity) throws Exception {
		httpSecurity.csrf().disable();
	}
    해주면 완전끝
=> 403은 '권한'에러임. '인증'된 클라이언트가 권한이 없는 자원에 접근할때 발생. 해결법은 2가지다. 토큰을 서버로 보내주든지, csrf 를 사용하지 않든지.
우선 스프링 시큐리티 4 부터는 CSRF 방지 기능이 디폴트로 작동한다. 스프링 시큐리티 3은 선택이였다. 그래서 CSRF 태그를 명시하지 않으면 적용되지 않았다. 하지만, 스프링 시큐리티 4는 디폴트이기때문에 필수로 적용된다. 만약 CSRF를 사용하고 싶지 않다면, CSRF 태그에 disabled="true" 를 명시하면 된다.
출처: https://to-dy.tistory.com/76 [todyDev]

스프링 공식사이트의 답변
POST를 수행할 때 403 Forbidden이 표시됩니다.
HTTP POST에 대해 HTTP 403 Forbidden이 반환되지만 HTTP GET에 대해서는 작동하는 경우 문제는 CSRF 와 관련이 있을 가능성이 큽니다 . CSRF 토큰을 제공하거나 CSRF 보호를 비활성화합니다(권장하지 않음).
@EnableWebSecurity에 의해서 Spring Security가 설정되면 기본적으로 CSRF 프로텍션은 활성화되므로 http.csrf(); 이 설정은 사실상 필요없습니다
https://wave1994.tistory.com/150
https://benfatto.tistory.com/46
https://zgundam.tistory.com/60

1. var uri = /*[[ @{/board/delete.do} ]]*/; 소스에서 expression expected 에러
원인 : 문제가 없는 코드임에도 이클립스 구문 validation에서 빨간딱지
아래와같이 뒤에 null 추가해주시면 빨간딱지 없어짐.
var uri = /*[[ @{/board/delete.do} ]]*/ null ;


☆☆
1. 타임리프 caused by: org.thymeleaf.exceptions.TemplateProcessingException : Exception evaluating SpringEL expression : "row.post_number?"
=> null이 발생할만한거 앞에 ? 붙여주면된다.
예를들어 <td th:text="${content.category.name}">Computer</td>
랐으면
<td th:text="${content.category?.name}">Computer</td>
이런식으로.





1. 마이바티스 매퍼 연결
   ☆☆☆Unsatisfied dependency expressed through bean property 'sqlSessionFactory
org.springframework.beans.factory.UnsatisfiedDependencyException: Error creating bean with name 'boardController': Unsatisfied dependency expressed through field 'boardService';
=>xml의 namespace를 풀경로로 지정
DBConfiguration의 매펴경로에 경로나 이름이 xml의 경로나 이름과 다른부분이 있다.
namespace에서 f3누리면 인터페이스로 잘 찾아가는데 빈등록이 안됨. ㅠㅠㅠ
@Mapper를 보고 namespace이름과 같은 애를 찾아서
인터페이스만으로도 dao 구현없이 사용할려는 약속
=> 패키지 경로가 문제였음


1. gson gradle에 추가할때 에러
FAILURE: Build failed with an exception.

* What went wrong:
Could not open cp_init remapped class cache for 6a5krabdzij62jglcll542e66 (C:\Users\ggoom\.gradle\caches\6.0.1\scripts-remapped\eclipsePlugin_eu89pja1z0urssza4vskwy6b1\6a5krabdzij62jglcll542e66\cp_init27b7120d8bed45e60dd0b95b8e5ecea0).
> Could not open cp_init generic class cache for initialization script 'F:\Programming\workspace_sts4\.metadata\.plugins\org.eclipse.buildship.core\init.d\eclipsePlugin.gradle' (C:\Users\ggoom\.gradle\caches\6.0.1\scripts\6a5krabdzij62jglcll542e66\cp_init\cp_init27b7120d8bed45e60dd0b95b8e5ecea0).
   > Could not initialize class org.codehaus.groovy.classgen.Verifier

* Try:
Run with --stacktrace option to get the stack trace. Run with --info or --debug option to get more log output. Run with --scan to get full insights.

* Get more help at https://help.gradle.org

BUILD FAILED in 33ms
==>https://search.maven.org/artifact/com.google.code.gson/gson/2.8.8/jar
수작업으로 라이브러리 추가해줬음



- The new driver class is `com.mysql.cj.jdbc.Driver'. log4jdbc
=>(해결됨) 최신버전 패치되어서 일어난일
https://dev-overload.tistory.com/28


- Exception in thread "restartedMain"
=> 이게 원인이 아니였고 NoClassDefFoundError: com/google/gson/JsonElement 이게 원인인것같은데 못고침

-could not run phased build action using Gradle distribution 'https://services.gradle.org/distributions/
=>
https://gabrielyj.tistory.com/201
C:\Users\사용자이름\.gradle\wrapper  에서 사용하지 않는 그래들버전 삭제
아래의 cmd에서 그래들 버전을 스플이부트에 맞는 버전으로 변경해주니 해결됨.


- gradle, jdk, boot 버전 호환시키기
=> https://willbfine.tistory.com/460
오 원인 좀 찾음
해당 소스에서 cmd열어서 ./gradlew build해보니까 자세한 정보 나옴.
* Where:
Build file 'F:\Programming\백엔드\스프링과 스프링부트\스프링부트 그래들 타임리프\타임리프 진행과정 백업\0908 백업13 트랜잭션\thymeleaf\build.gradle' line: 2

* What went wrong:
An exception occurred applying plugin request [id: 'org.springframework.boot', version: '2.5.3']
> Failed to apply plugin [id 'org.springframework.boot']
   > Spring Boot plugin requires Gradle 6.8.x, 6.9.x, or 7.x. The current version is Gradle 6.0.1
[스프링부트 gradle버전 변경](https://velog.io/@deepen/%EC%8A%A4%ED%94%84%EB%A7%81%EB%B6%80%ED%8A%B8-Gradle-%EB%B2%84%EC%A0%84-%EB%B3%80%EA%B2%BD%ED%95%98%EA%B8%B0)
[그래들 래퍼 설정](https://park-jongseok.github.io/languages/java/2019/11/01/upgrade-gradle.html)
gradle-wrapper.properties 의 내용 변경됨.
[해결한 설정파일](https://www.codegrepper.com/code-examples/whatever/gson+gradle+dependency)
Running with Spring Boot v2.5.3, Spring v5.3.9


- ★★잘되던 db연결 안되고 workbench에서도 연결안되는거 확인
=> Could not acquire management access for administration
윈도우 + r (실행) 들어가서 services.msc  가서 mysql80시작


- ★★★Could not find method compile() for arguments [commons-io:commons-io:2.11.0] on object of type org.gradle.api.internal.artifacts.dsl.dependencies.DefaultDependencyHandler.
=> 현재버전의 그래들에는 그 함수없음.
compile, runtime, testCompile, testRuntime 없어지고 Gradle을 4.10 ( 2018년 8월 27일 ),
그리고 마지막에 제거 Gradle을 7.0 ( 2021년 4월 9일 )
implementation, runtimeOnly, testImplementation, 및 testRuntimeOnly 사용

- log4j cannot find dtd 에 xml파일 많은 에러
=> dtd참조한줄 재정의
https://languagestory.tistory.com/44

- ★★★ 프로젝트 받았는데 실행안되고 run configuraion 뜰때 ★★★
=> 해결.
nested 프로젝트 우클릭 configure - Configure and Detected Nested Project로 정확한 하위프로젝트 지정.
또는 여기에 gradle 있으면 바로 선택.

- ★★mybatis db에서는 잘 출력되는데 자바단에서 갯수도 잘 갖고오는데 null null 뜰때★★
=> (해결)커밋 확인 후
마이바티스 카멜케이스 설정했다면 DTO의 카멜케이스 확인.

- path contains invalid character
프로젝트위치한 경로에 한글이 있어서 기본내장 xml parser에 한글 해석하는부분이 부족하니 dependency 추가

- Missing artifact javax.servlet:servlet-api:jar:3.1.0
=> (해결) 해결  : servlet 디펜던시 artifacId에 javax. 추가
   https://moon1226.tistory.com/82

- mysql은 pom.xml에만 해놓고 커드 어디에서도 쓰지않는데 Loading class `com.mysql.jdbc.Driver'. This is deprecated. The new driver class is `com.mysql.cj.jdbc.Driver'. The driver is automatically registered via the SPI and manual loading of the driver class is generally unnecessary.
=>

- ★★★ db connection할때
java.lang.ClassNotFoundException: oracle.jdbc.driver.OracleDriver
=> (해결)  https://earth-ing.tistory.com/25
1. 프로젝트 우클릭 - java build path - libraries - ojdbc 라이브러리 추가.
2. 프로젝트 우클릭 - properties - Deployment Assembly - Add - Java Build Path Entries에 ojdbc 추가

- ★★★ datasource is null
=> 설정 정보가 다 맞다면, 설정어노테이션이 빠져있을 가능성이 높음.

-  ★★★ ERROR: jdbc.audit - 1. PreparedStatement.setNull(3, 1111) ★★★
  ★★★java.sql.SQLException: 부적합한 열 유형: 1111★★★
=> mybatis와 오라클을 사용할때 null이 와서는 안될컬럼에 null이 들어와서 생긴일
간편하게 고칠려면 , #{timestampAcc, jdbcType=VARCHAR}
이런식으로 동적값넣어주는곳 뒤에 추가해주면되는데
설정으로 바꿀려면 context-mapper.xml 파일을 열면 property name이 configLocation 경로를 찾아가서
<setting name="jdbcTypeForNull" value="NULL" />
해줘야한다.


- Ajax 사용할때 Required String parameter 'userId' is not present
=> 원인 : @RequestParam이나 기타 Prameter 값을 받아올 때 null이거나 Type이 맞지 않는 경우 이 에러가 발생합니다.
해결 : public int idCheck(@RequestParam(required = false) String userId) {  이런식으로 화면에서 넘겨준 데이터 제대로 받아오게 하면됨.

- A query was run and no Result Maps were found
=> 원인 : mybatis에서 resultType을 명시해주지 않았음. CUD는 없어도되지만 R에는 반드시 있어야한다.

- 위에서 <select id="idCheck" resultType="_int"> 이렇게 바꿔줬는데
No converter found for return value of type: class java.lang.Integer
=> 원인 : json을 해당 타입으로 변환시키지 못해서.
해결: pom.xml에 jackson같은 json라이브러리 추가해주면됨. Databind
